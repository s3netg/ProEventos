/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormat.pipePipe } from './DateTimeFormat.pipe.pipe';

describe('Pipe: DateTimeFormat.pipee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormat.pipePipe();
    expect(pipe).toBeTruthy();
  });
});
